name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-x64
            cmake_flags: ""
          - os: macos-latest
            name: macos-x64
            cmake_flags: ""
          - os: windows-latest
            name: windows-x64
            cmake_flags: ""

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install LLVM (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt update
        sudo apt install -y cmake llvm-dev clang build-essential

    - name: Install LLVM (macOS)
      if: matrix.os == 'macos-latest'
      run: |
        brew install cmake llvm
        # Find the actual LLVM installation path
        LLVM_PATH=$(brew --prefix llvm)
        echo "LLVM installed at: $LLVM_PATH"
        echo "Checking for LLVMConfig.cmake..."
        find "$LLVM_PATH" -name "LLVMConfig.cmake" -type f 2>/dev/null || echo "LLVMConfig.cmake not found"
        find "$LLVM_PATH" -name "*LLVM*" -type d 2>/dev/null | head -5
        # Set the environment variable
        CMAKE_DIR=$(find "$LLVM_PATH" -name "LLVMConfig.cmake" -type f | head -1 | xargs dirname 2>/dev/null)
        if [ -n "$CMAKE_DIR" ]; then
          echo "Found LLVM CMake dir: $CMAKE_DIR"
          echo "LLVM_DIR=$CMAKE_DIR" >> $GITHUB_ENV
        else
          echo "Fallback to standard path"
          echo "LLVM_DIR=${LLVM_PATH}/lib/cmake/llvm" >> $GITHUB_ENV
        fi
        echo "${LLVM_PATH}/bin" >> $GITHUB_PATH

    - name: Install LLVM (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        choco install cmake llvm --force
        echo "LLVM_DIR=C:/Program Files/LLVM/lib/cmake/llvm" >> $env:GITHUB_ENV
        echo "C:/Program Files/LLVM/bin" >> $env:GITHUB_PATH

    - name: Configure CMake (Unix)
      if: matrix.os != 'windows-latest'
      run: |
        mkdir build
        cd build
        echo "Current LLVM_DIR: $LLVM_DIR"
        echo "Checking if LLVM_DIR exists:"
        ls -la "$LLVM_DIR" || echo "LLVM_DIR path does not exist"
        if [ "${{ matrix.os }}" = "macos-latest" ]; then
          cmake -DCMAKE_BUILD_TYPE=Release -DLLVM_DIR="${LLVM_DIR}" ..
        else
          cmake -DCMAKE_BUILD_TYPE=Release ..
        fi

    - name: Configure CMake (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        mkdir build
        cd build
        cmake -DCMAKE_BUILD_TYPE=Release -DLLVM_DIR="${env:LLVM_DIR}" -G "Visual Studio 17 2022" ..

    - name: Build
      run: |
        cd build
        cmake --build . --config Release

    - name: Test
      run: |
        cd build
        ctest --output-on-failure -C Release

    - name: Package (Unix)
      if: matrix.os != 'windows-latest'
      run: |
        cd build
        mkdir -p ../release/gloin-${{ matrix.name }}
        cp gloinc ../release/gloin-${{ matrix.name }}/
        cp -r ../examples ../release/gloin-${{ matrix.name }}/
        cp ../README.md ../release/gloin-${{ matrix.name }}/
        cp ../LICENSE ../release/gloin-${{ matrix.name }}/
        cd ../release
        tar -czf gloin-${{ matrix.name }}.tar.gz gloin-${{ matrix.name }}/

    - name: Package (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        cd build
        mkdir -p ../release/gloin-${{ matrix.name }}
        copy Release/gloinc.exe ../release/gloin-${{ matrix.name }}/
        xcopy /E /I ../examples ../release/gloin-${{ matrix.name }}/examples
        copy ../README.md ../release/gloin-${{ matrix.name }}/
        copy ../LICENSE ../release/gloin-${{ matrix.name }}/
        cd ../release
        7z a gloin-${{ matrix.name }}.zip gloin-${{ matrix.name }}/

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: gloin-${{ matrix.name }}
        path: |
          release/gloin-${{ matrix.name }}.tar.gz
          release/gloin-${{ matrix.name }}.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && always()
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/
      continue-on-error: true

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}
        release_name: Gloin Compiler ${{ github.ref_name }}
        body: |
          # Gloin Programming Language Compiler ${{ github.ref_name }}
          
          This is a pre-release alpha version of the Gloin programming language compiler.
          
          ## What's Included
          - ✅ Complete compilation pipeline (Source → LLVM IR → Executable)
          - ✅ Core language features: variables, functions, structs, pointers
          - ✅ Import system: standard library, local modules, external packages
          - ✅ Standard library: I/O functions, type conversions
          - ✅ Multiple compiler modes: production, AST analysis, debug
          - ✅ Project management: `gloinc init` for new projects
          
          ## Platform Support
          - **Linux x64**: Full support - pre-built binary included
          - **macOS x64**: Full support - pre-built binary included  
          - **Windows x64**: Build from source (pre-built binary coming in next release)
          
          ## Installation
          
          ### Linux & macOS Users
          1. Download the appropriate package for your platform
          2. Extract: `tar -xzf gloin-[platform].tar.gz`
          3. Add to PATH or run directly: `./gloinc --help`
          
          ### Windows Users  
          For now, please build from source:
          ```bash
          git clone https://github.com/gloinlang/compiler.git
          cd compiler
          # Install LLVM, CMake, and Visual Studio
          mkdir build && cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          cmake --build . --config Release
          ```
          
          ## Quick Start
          ```bash
          # Create a hello world program
          echo 'import "@std"
          def main() -> i32 {
              std.println("Hello, World!");
              return 0;
          }' > hello.gloin
          
          # Compile and run
          gloinc hello.gloin
          ./hello
          ```
          
          ## Current Limitations
          - 🚧 Some advanced syntax features still in development
          - 🚧 Standard library is basic but functional
          - 🚧 Error messages can be improved
          - 🚧 This is alpha software - expect rough edges
          
          ## Documentation
          Complete documentation and examples are included in the package and available at:
          https://github.com/gloinlang/compiler
          
          ## Feedback
          Please report issues and feedback at: https://github.com/gloinlang/compiler/issues
          
          ---
          
          **Note**: This is alpha software. While functional, it's still under active development.
        draft: false
        prerelease: true

    - name: Upload Linux Release Asset
      uses: actions/upload-release-asset@v1
      if: hashFiles('artifacts/gloin-linux-x64/gloin-linux-x64.tar.gz') != ''
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: artifacts/gloin-linux-x64/gloin-linux-x64.tar.gz
        asset_name: gloin-linux-x64.tar.gz
        asset_content_type: application/gzip

    - name: Upload macOS Release Asset
      uses: actions/upload-release-asset@v1
      if: hashFiles('artifacts/gloin-macos-x64/gloin-macos-x64.tar.gz') != ''
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: artifacts/gloin-macos-x64/gloin-macos-x64.tar.gz
        asset_name: gloin-macos-x64.tar.gz
        asset_content_type: application/gzip

    - name: Upload Windows Release Asset
      uses: actions/upload-release-asset@v1
      if: hashFiles('artifacts/gloin-windows-x64/gloin-windows-x64.zip') != ''
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: artifacts/gloin-windows-x64/gloin-windows-x64.zip
        asset_name: gloin-windows-x64.zip
        asset_content_type: application/zip